{
  "name": "audit-scripts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "tsx index.ts",
    "contract-drift": "tsx check-contract-drift.ts",
    "contract-drift:update": "tsx check-contract-drift.ts --update-baselines",
    "test": "node --test --import tsx \"tests/**/*.test.ts\"",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@types/node": "^26.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.3"
  }
}
